# Copy this file to ".env" (same folder) and fill in your own key.
# Never commit the real ".env" file.

OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown)
UDA_HUB_CHAT_MODEL=gpt-4o-mini
UDA_HUB_EMBEDDING_MODEL=text-embedding-3-small
